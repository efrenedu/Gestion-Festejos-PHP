folder with the private files of server. 
-Instructions:
  1-Rename the files of config and tokens .json.example as .json
  2-Change the Credentials and Adapt to your Server Data of config.json
  3-Chnge the Tokens of tokens files, ensure the key of each token file is different
